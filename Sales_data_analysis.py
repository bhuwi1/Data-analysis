{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.7.10","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"import pandas as pd\nimport numpy as np\nimport matplotlib.pyplot as plt\nimport os\n\nfiles = [file for file in os.listdir('')]\nfor file in files:\n    print (file)\n    \npath = ''\nall_data = pd.DataFrame()\nfor file in files:\n    current_df = pd.read_csv(path+'/'+file)\n    all_data = pd.concat([all_data, current_df])\nall_date.shape\nall_data.to_csv(path+'/'+all_data, index=False)\n\n# part 2\nall_data.head()\nall_data.isnull().sum()\nall_data = all_data.dropna(how = 'all')\nall_data.shape\n'04/19/19 08:46'.split('/')[0]\n\ndef month(x):\n    return x.split('/')[0]\nall_data['month'] = all_data['Order Date'].apply(month)\nall_data.head()\nall_data.dtypes\nall_data['month'] = all_data['month'].astype(int)\nall_data['month'].unique()\n\nfilter = all_data['month']=='Order Data'\nall_data = all_data[~filter]\nall_data.head()\n\nall_type['month'] = all_data['month'].astypes(int)\nall_data.dtypes\nall_data['Quality Ordered'] = all_data['Quality Ordered'].astypes(int)\nall_data['Price Each'] = all_data['Price Each'].astypes(float)\n\nall_data['sales'] = all_data['Quality Ordered']*all_data['Price Each']\nall_data.head()\n\nall_data.groupby('month')['sales'].sum()\nmonths = range(1,13)\nplt.bar(months,all_data.groupby('month')['sales'].sum())\nplt.xticks(month)\nplt.xlabel('month')\nplt.ylabel('Sales in USD')\n\n# part3\nall_data.head()\n\ndef city(x):\n    return x.split(',')[1]\nall_data['city'] = all_data['Purchase Address'].apply(city)\nall_data.head()\n\nall_data.groupby('city')['city'].count().plot.bar()\nall_data['Order Data'].dtype\n\nall_data['Hour'] = pd.to_determine(all_data['Order data']).dt.hour\n\nkeys = []\nhour = []\nfor key, hour_df in all_data.groupby('Hour'):\n    keys.append(key)\n    hour.append(len(hour_df))\n    \nplt.grid()\nplt.plot(keys, hour)\n\n#part 4\nall_data.groupby('Product')['Quantity Ordered'].sum().plot(kind = 'bar')\nall_data.groupby('Product')['Price Each'].mean()\n\nproducts = all_data.groupby('Product')['Quantity Ordered'].sum().index\nquantity = all_data.groupby('Product')['Quantity Ordered'].sum()\nprices = all_data.groupby('Product')['Price Each'].mean()\n\nfig,ax1 = plt.subplots()\nax2 = ax1.twinx()\nax1.bar(products, quantity, color = 'g')\nax2.plot(products, prices)\nax1.set_xticklabels(products, rotation = 'vertical',size=8)\n\n\ndf = all_data['Order ID'].duplicate(keep=False)\ndf2 = all_data[df]\ndf2.head()\n\ndf2['Grouped']=df2.groupby('Order ID')['Product'].tansform(lambda x:','.join(x))\ndf2 = df2.drop_duplicate(subset = ['Order ID'])\ndf2.head()\n\ndf2['Grouped'].value_counts()[0:5].plot.pie()\n\n\n\n\n\n\n\n  \n\n\n\n\n\n\n","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true},"execution_count":null,"outputs":[]}]}